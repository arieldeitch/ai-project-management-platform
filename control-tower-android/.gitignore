# Gradle / build output
.gradle/
build/
local.properties
*.iml
.idea/

# Firebase Android client config is tracked intentionally.
# Firebase documents google-services.json as containing non-secret app/project identifiers.
# NEVER commit Firebase service-account JSON / private keys.
*.jks
*.keystore
